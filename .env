# .env (example) — do NOT commit real keys publicly
FLASK_SECRET=replace_with_random_string
IP_LOOKUP_API=         # optional (not required for ip-api.com)
LLM_API_KEY=           # optional if using LLM later
